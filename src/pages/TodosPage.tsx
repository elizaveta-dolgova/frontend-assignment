import {TodoWrapper} from '../components/todos/TodoWrapper';
import {TodosContent} from '../components/todos/List/TodosContent';

export function TodosPage() {
  return (
    <TodoWrapper>
      <TodosContent />
    </TodoWrapper>
  );
}
